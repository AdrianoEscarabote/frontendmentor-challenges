import { Meta, StoryObj } from "@storybook/react"
import MaintainSchedule from "."

export default {
  title: "components/maintainSchedule",
  component: MaintainSchedule,
  tags: ["autodocs"],
} as Meta

export const Primary: StoryObj = {}
