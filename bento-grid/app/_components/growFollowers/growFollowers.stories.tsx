import { Meta, StoryObj } from "@storybook/react"
import GrowFollowers from "."

export default {
  title: "components/growFollowers",
  component: GrowFollowers,
  tags: ["autodocs"],
} as Meta

export const Primary: StoryObj = {}
