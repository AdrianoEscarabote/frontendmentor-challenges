import { Meta, StoryObj } from "@storybook/react"
import ManageAccounts from "."

export default {
  title: "components/manageAccounts",
  component: ManageAccounts,
  tags: ["autodocs"],
} as Meta

export const Primary: StoryObj = {}
