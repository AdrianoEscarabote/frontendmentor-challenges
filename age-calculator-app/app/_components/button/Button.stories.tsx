import { Meta, StoryObj } from "@storybook/react"
import Button from "."

export default {
  title: "components/Button",
  component: Button,
} as Meta

/** Button Component */
export const Primary: StoryObj = {}
