import { Meta, StoryObj } from "@storybook/react"
import input from "."

export default {
  title: "components/Input",
  component: input,
  tags: ["autodocs"],
} as Meta

export const Primary: StoryObj = {}
