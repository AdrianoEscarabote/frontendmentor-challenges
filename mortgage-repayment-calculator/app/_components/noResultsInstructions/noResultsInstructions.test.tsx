import { render } from "@testing-library/react"
import NoResultsInstructions from "."

describe("No Results Instructions Component", () => {
  it("should render correctly", () => {
    render(<NoResultsInstructions />)
  })
})
